name: create-docker-image-backend

on:
  push:
    branches:
      - master
    paths:
      - 'app/**'
      - 'Dockerfile'
      - 'requirements.txt'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: clone code
        uses: actions/checkout@v4

      - name: setup docker buildx
        uses: docker/setup-buildx-action@v3

      - name: login docker hub
        uses: docker/login-action@v3.4.0
        with:
          username: ${{secrets.DOCKERHUB_USERNAME}}
          password: ${{secrets.DOCKERHUB_TOKEN}}

      - name: calculate tag version for dockerhub
        run: echo 'NEW_VERSION=$(( ${{github.run_number}} + 2))' >> $GITHUB_ENV

      - name: docker image build
        uses: docker/build-push-action@v5
        with:
          context: ./app
          file: ./app/Dockerfile
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/finance-management-backend:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/finance-management-backend:v${{ github.run_number }}

      - name: trigger on koyeb
        run: |
          curl -X POST https://app.koyeb.com/v1/services/${{ secrets.KOYEB_ID_SERVICE }}/redeploy \
            -H "Authorization: Bearer ${{ secrets.KOYEB_API_TOKEN }}"
